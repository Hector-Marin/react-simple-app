import React from 'react';

const ServerError = (props) => (
    <div className="container text-center">
        <h1>Sorry baby, the server isn't available</h1>
        <p>Try to do that in a few minutes.</p>
    </div>
);

export default ServerError;